defaults:
  - topic_corpus

data_load:
  data_file: ${oc.select:global.datasets_path,datasets}/processed/khmer_tokenized/train.parquet
id_col: id
text_col: nouns
timestamp_col: time
ngrams:
  max_len: 3
verbose: true
