# @package _global_
defaults:
  - __init__
  - /fetcher@khmer_all: khmer_all
  - /task@nbcpu-datasets: nbcpu-datasets
  - /runner@nbcpu-topic_nouns: nbcpu-topic_nouns
  - override /project: nbcpu

_config_name_: nbcpu
verbose: true
tasks:
  # - khmer_all
  # - nbcpu-datasets
  - nbcpu-topic_nouns

global:
  num_workers: 100
  datasets_path: ${__get_path__:datasets}
